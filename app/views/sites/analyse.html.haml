%section#site_analyse

  %h1 
    = @site.url

  %iframe{:src => "#{Settings.host}/#{Settings.sites_folder}/#{@site.token}/current/index.html", :width => "100%", :id => "crawled_site"}

  %p= link_to "Zurück in die Zukunft! (oder einfach weiter zur Timeline)", sites_timeline_path(@site)